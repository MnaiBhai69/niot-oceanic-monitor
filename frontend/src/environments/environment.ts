export const environment = {
  production: false,
  // API calls go through Angular proxy → backend at localhost:3000
  // To change the backend port, edit proxy.conf.json only — not this file
  apiUrl: '/api'
};
